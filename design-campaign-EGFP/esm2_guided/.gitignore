# Scratch output from the pre-flight checks: check_scale.py writes _probe/, and the retired
# ../archive/check_lam_bright0.py writes _equiv/ if it is ever re-run from here. Both probe the
# sampler and throw the designs away; only their printed tables matter.
_probe/
_equiv/
